# Next Tasks

## Current Status

The repository is a public static EnergizeOS documentation site with a root `index.html`, a `CNAME` for `docs.energizeos.com`, and public metadata already present in the HTML. The README is currently minimal.

## Next Recommended Tasks

1. Verify GitHub Pages settings and confirm the production publishing source for `docs.energizeos.com`.
2. Improve `README.md` with the site purpose, domain, structure, and safe editing rules.
3. Confirm every public document link in `index.html` resolves correctly.
4. Check mobile, tablet, and desktop layouts for horizontal scroll before the next public release.
5. Confirm `vi/assets/og-1200x630.png` exists and renders correctly when shared.
6. Document whether this repository should remain pure static HTML or eventually use a controlled generator.

## Do Not Do

- Do not add dependencies.
- Do not add `package.json`.
- Do not change public routes.
- Do not change `CNAME` without explicit approval.
- Do not replace EnergizeOS VI with generic SaaS styling.
- Do not use a dark scheme unless explicitly approved.
- Do not expose private credentials or internal implementation logic.

## Handoff Prompt

Continue this repository from its project context.

First read:
1. CLAUDE.md
2. docs/project-brief.md
3. docs/decision-log.md
4. docs/todo-next.md
5. docs/acceptance-checklist.md

Then summarize the current state in no more than 8 bullets and execute only the next task listed in docs/todo-next.md. Do not change scope.
